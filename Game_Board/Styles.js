import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    margin: 10,
    justifyContent: "space-between",
  },
  difficultyText: {
    marginLeft: 5,
  },
  timerStyle: {
    marginRight: 5,
  },
});

export { styles };
